# services/shared/requirements.txt
# Installed in every Python service virtual environment

# Core FastAPI stack
fastapi>=0.115.2
uvicorn[standard]==0.30.1
pydantic==2.8.2
pydantic-settings==2.3.4
python-dotenv==1.0.1
python-multipart>=0.0.18

# Async HTTP client (inter-service + Ollama calls)
httpx==0.27.2

# Async MongoDB driver
motor==3.5.1
pymongo==4.8.0

# Cache
redis==5.0.8

# Vector store (embedded in RAG service process)
chromadb==0.5.20